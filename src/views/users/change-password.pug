extends ../base

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="oldPassword", placeholder="old password", required, type="password")
        input(name="newPassword", placeholder="New Password", required, type="password")
        input(name="newPassword2", placeholder="New Password Confirmation", required, type="password")
        input(type="submit", value="Save")

